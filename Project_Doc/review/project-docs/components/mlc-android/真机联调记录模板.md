# App 本地 AI 真机联调记录模板

本文档用于记录单台 Android 设备的一次完整联调结果。

建议一台设备、一版基座、一份记录。

---

## 1. 基本信息

| 项目 | 记录值 |
| --- | --- |
| 记录日期 |  |
| 记录人 |  |
| Git 提交/分支 |  |
| App 包名/版本 |  |
| HBuilderX 版本 |  |
| 自定义基座版本 |  |
| 测试环境 | 开发 / 测试 / 预发 |
| `adb` 预检命令 | `pnpm check:app-local-ai-adb -- --device <serial>` 或 `pnpm check:app-local-ai-adb -- --device <serial> --package <包名>` |
| 设备目录自检命令 | `pnpm check:app-local-ai-device-models -- --device <serial>` |
| 一键采集命令 | `pnpm collect:app-local-ai-device-report -- --device <serial>` |
| 一键产物总装命令 | `pnpm finalize:app-local-ai-device-report -- --device <serial>` |
| 一键总装并同步汇总表 | `pnpm finalize:app-local-ai-device-report -- --device <serial> --sync-table` |
| 总装并合并联调信息 | `pnpm finalize:app-local-ai-device-report -- --device <serial> --diagnostics-input <联调信息文本路径> --sync-table` |
| 合并联调信息命令 | `pnpm merge:app-local-ai-runtime-diagnostics -- --json <摘要路径> --input <联调信息文本路径>` |
| 预填记录命令 | `pnpm prefill:app-local-ai-device-record -- --latest` |
| 汇总建议行命令 | `pnpm summarize:app-local-ai-device-report -- --latest` |
| 同步汇总表命令 | `pnpm sync:app-local-ai-summary-table -- --latest` |
| `logcat` 抓取命令 | `pnpm logcat:app-local-ai -- --device <serial> --clear` |
| 采集报告路径 |  |

## 2. 设备信息

| 项目 | 记录值 |
| --- | --- |
| 设备品牌 |  |
| 设备型号 |  |
| Android 版本 |  |
| CPU ABI |  |
| 可用存储空间 |  |
| 网络环境 | Wi-Fi / 4G / 5G |
| 设备序列号 |  |

## 3. 本次联调目标

勾选本次要验证的范围：

- [ ] 原生桥接识别
- [ ] `MLC` 运行时资产识别
- [ ] `JSONFFIEngine` 就绪
- [ ] 模型下载
- [ ] 本地聊天推理
- [ ] 本地会话持久化
- [ ] 模型删除与清理

## 4. 运行时资产准备记录

| 项目 | 结果 | 备注 |
| --- | --- | --- |
| 已执行 `pnpm generate:mlc-config` | 是 / 否 |  |
| 已执行 `mlc_llm package` | 是 / 否 |  |
| `dist/lib/mlc4j` 已生成 | 是 / 否 |  |
| 已执行 `pnpm prepare:mlc-android-runtime -- --source ...` | 是 / 否 |  |
| `assets/mlc-app-config.json` 已同步 | 是 / 否 |  |
| `libs/*.jar` 已同步 | 是 / 否 |  |
| `libs/<abi>/*.so` 已同步 | 是 / 否 |  |
| `config.json` ABI 正确 | 是 / 否 |  |
| 已执行 `pnpm check:app-local-ai-adb` | 是 / 否 |  |
| 已执行 `pnpm check:app-local-ai-device-models` | 是 / 否 |  |

## 4.1 ADB 预检结果

| 项目 | 结果 | 备注 |
| --- | --- | --- |
| `adb` 可用 | 是 / 否 |  |
| 设备 ABI 与 `arm64-v8a` 匹配 | 是 / 否 |  |
| 包名已安装 | 是 / 否 |  |
| `nativeLibraryDir` 已识别 | 是 / 否 |  |
| 常见 `_doc/help-local-llm` 目录已识别 | 是 / 否 / 未生成 |  |

`adb` 预检输出摘要：

```text

```

`logcat` 关键输出摘要：

```text

```

设备目录自检输出摘要：

```text

```

## 5. App 诊断卡片记录

联调入口：

- `聊天 -> 本地模式 -> 本地模型`

记录页面显示内容：

| 项目 | 结果 | 备注 |
| --- | --- | --- |
| `nativeBridgeDetected` | true / false |  |
| `packagedRuntimeDetected` | true / false / 空 |  |
| `runtimeReady` | true / false / 空 |  |
| 模型根目录 |  |  |
| 已准备模型数 |  |  |
| 可用模型 |  |  |
| 待修复模型 |  |  |
| 页面提示文案 |  |  |

复制联调信息原文：

```text

```

已准备模型目录明细：

| 模型 | runtimeModelId | 目录状态 | 已就绪文件数 | 缺失文件 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |
|  |  |  |  |  |

截图路径：

- 诊断卡片截图：``

## 6. 模型下载记录

建议至少验证 1 个小模型。

| 项目 | 结果 | 备注 |
| --- | --- | --- |
| 测试模型 ID |  |  |
| 点击下载后有加载态 | 通过 / 失败 |  |
| 下载完成后变为已下载 | 通过 / 失败 |  |
| 待修复模型仍显示在已下载列表 | 通过 / 失败 / 未测 |  |
| 待修复模型可直接修复 | 通过 / 失败 / 未测 |  |
| 模型目录完整 | 通过 / 失败 | 目录状态： |
| 激活模型成功 | 通过 / 失败 |  |
| 删除模型成功 | 通过 / 失败 |  |
| 删除后目录已清理 | 通过 / 失败 |  |

## 7. 本地聊天记录

| 项目 | 结果 | 备注 |
| --- | --- | --- |
| 成功进入本地聊天页 | 通过 / 失败 |  |
| 首次消息成功返回 | 通过 / 失败 | 首次响应耗时： |
| 连续 3 轮对话稳定 | 通过 / 失败 |  |
| 返回首页后最近聊天可见 | 通过 / 失败 |  |
| 重启 App 后本地会话仍在 | 通过 / 失败 |  |
| 本地模式未写入服务端会话 | 通过 / 失败 |  |

本轮样例问题输入：

1. ``
2. ``
3. ``

本轮输出质量备注：

-

## 8. 性能与稳定性观察

| 项目 | 结果 | 备注 |
| --- | --- | --- |
| 首次模型准备耗时可接受 | 通过 / 失败 |  |
| 推理期间页面无明显卡死 | 通过 / 失败 |  |
| 未出现闪退/白屏 | 通过 / 失败 |  |
| 未出现 OOM / native crash | 通过 / 失败 |  |
| 中断后可恢复继续对话 | 通过 / 失败 |  |

## 9. 问题记录

| 编号 | 现象 | 复现步骤 | 严重级别 | 初步判断 |
| --- | --- | --- | --- | --- |
| 1 |  |  | P0 / P1 / P2 |  |
| 2 |  |  | P0 / P1 / P2 |  |

## 10. 结论

本轮结论：

- [ ] 可进入下一阶段联调
- [ ] 可作为当前设备通过记录
- [ ] 需要先修复阻塞问题再继续

结论说明：

-

## 11. 建议附加材料

建议和本记录一起保留：

- 诊断卡片截图
- 模型下载完成截图
- 本地聊天成功截图
- `mlc_llm package` 输出摘要
- 自定义基座构建说明或版本号
